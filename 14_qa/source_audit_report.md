# Source audit report - Revision F

> FICTIONAL ENGINEERING PROJECT — NOT FOR CONSTRUCTION

> CONCEPTUAL SAFETY ARCHITECTURE — REQUIRES PROJECT-SPECIFIC RISK ASSESSMENT, DESIGN, VERIFICATION AND VALIDATION BY A QUALIFIED MACHINERY-SAFETY ENGINEER. NO PERFORMANCE LEVEL, SIL, CATEGORY, CE CONFORMITY OR REGULATORY COMPLIANCE IS CLAIMED.

Revision-F source review covers the inherited restart/replay/disposition invariants plus the additive 47-node contract, exact capture acknowledgement, processing state, health, model/dataset/calibration identity, immutable result publication, PLC-derived result age, confidence/reason/disposition checks and fail-closed restart/reconnect behavior.

The edge foundation executes its bounded configuration schema, records the runtime/contract/schema SHA-256 values in structured logs, requires an append/flush/fsync inspection-audit write before result publication, publishes terminal state before RESULT_VALID, rejects active recipe/context change and excessive clock discontinuity, and keeps the unimplemented ONNX/recorded-input path permanently unauthorized. Dataset split/hash/leakage checks, evaluation math, local health/metrics and certificate-backed asyncua tests are controlled. Synthetic fixtures prove plumbing only.

Native TIA/WinCC/Startdrive/PLCSIM, production PLC/Jetson/PKI, representative data, trained model, target CUDA/TensorRT/DeepStream/TAO runtime, physical optics, site electrical inputs and qualified safety review remain external.

The first GitHub clean-clone reproduction of candidate a036620c5332997647625b510675345a33a16261 exposed an intermittent secure-session disconnect: asyncua transport supervision was incorrectly tied to the 10 ms PLC poll cadence and used a 50 ms server-state probe timeout. Revision F now gives the transport watchdog at least the configured OPC UA operation budget and a one-second floor. The added regression check plus eight repeated two-test secure runs and the complete 86-test edge suite passed after correction.

The next GitHub clean-clone reproduction of corrected candidate 9abc5d71024f42b07a680aac6b430e6945b4068a exposed a separate release-hygiene defect: FreeCAD's embedded Python cached the native verification script inside the controlled tree despite the parent bytecode environment setting. The reproduction workflow now executes a controlled copy from a unique system-temporary directory while binding the authoritative project root explicitly, then verifies and removes only that checked temporary directory. Native evidence still writes to the controlled paths and must remain byte-identical.

The subsequent GitHub clean-clone reproduction of candidate 647e545b13ca9759563138fbc44857263950392b reached the final clean-state assertion and exposed a line-ending mismatch in the structured vision-fault CSV: Python's default CSV dialect emitted CRLF while the controlled Git policy requires LF. The generator now sets an explicit LF line terminator, aligning generated working bytes with the authoritative Git blob and preventing platform-default newline drift.

The fourth GitHub clean-clone reproduction of candidate bb4e1d7225b64b02a6f7ed9c3c69844946318431 passed all engineering and native suites but changed six isolated RGB pixels in the 1,531-by-2,726 PLC-I/O sheet render and the derived contact sheet while the normalized workbook remained byte-identical. Candidate renders now stay in a unique system-temporary directory. Tolerance is enabled only when both the authoritative CSV aggregate and workbook-builder SHA-256 match the reviewed baseline. The release fails for any semantic-hash drift, missing/unexpected entry, non-regular expected file, dimension change, more than 16 differing pixels, a drift bounding box larger than 64 pixels or a differing-pixel fraction above 5e-6. A bounded renderer-only delta retains the previously reviewed Git PNG; any baseline update requires explicit `FC01_UPDATE_WORKBOOK_RENDERS=1` after visual review, and the verification-only reproduction rejects that variable before generation. Sixteen regression cases exercise the exact known defect, each isolated threshold, semantic binding, dimensions, missing/unexpected files and directories, and authorized-update paths.

THE NVIDIA VISION SUBSYSTEM IS NON-SAFETY-RELATED AND MUST NOT BE USED AS THE SOLE MEANS OF PERSONNEL PROTECTION, SAFE STOP, GUARD MONITORING OR HAZARDOUS-MOTION CONTROL.
